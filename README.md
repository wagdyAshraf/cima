git clone https://github.com/wagdyAshraf/cima.git >>> to clone git project
git checkout develop >>> to switch branch
git pull origin develop >>> to pull any update if found
git checkout -b feat/navbar-twd-351548 >>> to clone git project develop to work in it
git status >>> to show if any file changed
